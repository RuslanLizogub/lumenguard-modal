# Progress

## Виконано
- Реалізовано core-логіку: `check_ip`, `compare_states`, `format_ua_message`, `load_state`, `save_state`.
- Оркестрацію винесено в `src/lumenguard/runner.py`.
- Конфігурацію винесено в `src/lumenguard/config.py`.
- Створено окремий Modal entrypoint: `modal_app.py`.
- Локальний CLI залишено в `main.py` (без Modal-коду).
- Проєкт переведено на `pyproject.toml` замість `requirements.txt`.
- Налаштовано Telegram інтеграцію через `httpx` із `parse_mode="HTML"`.
- Додано підтримку часового поясу (`TIMEZONE`).
- Оновлено шаблон повідомлень під наочний формат `🟢/🔴 + ⏰ + ⏳`.
- Оновлено контракти інтеграцій у `docs/contracts`.
- Оновлено `README.md`, `workflow.md`, `main_logic.md`.
- Оновлено memory bank для актуального контексту.
- Тести пройдено (`pytest`).
- Smoke-run у Modal пройдено (`modal run modal_app.py::monitor_with_modal`).

## Поточний статус
- Система готова до експлуатації для 1+ цілей.
- Для отримання сповіщення потрібна фактична зміна статусу цілі.

## Наступний крок (опційно)
- Додати окремий health-check endpoint/метрики для зовнішнього моніторингу сервісу.
