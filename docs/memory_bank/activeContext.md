# Active Context

## Поточний стан
Проєкт у стабільному робочому стані: локальний режим перевірений, Modal Cron задеплоєний, повідомлення в Telegram надсилаються при зміні стану.

## Ядро логіки
- `src/lumenguard/logic.py`: TCP-перевірка, порівняння станів, форматування UA-повідомлень, JSON state helpers.
- `src/lumenguard/config.py`: валідація runtime-конфігурації через Pydantic.
- `src/lumenguard/runner.py`: оркестрація циклу, Telegram API, робота зі state.
- `main.py`: локальний CLI-вхід (`--once` або безперервний цикл).
- `modal_app.py`: Modal-вхід із `Cron`, `Dict`, `Secret`.
- `tests/test_logic.py`, `tests/test_runner.py`: покриття логіки і оркестратора.

## Фактичний формат повідомлень
- Онлайн:
  - `🟢 <b>Світло з'явилося</b>`
  - `⏰ Час появи: <b>HH:MM</b>`
  - `⏳ Світло було відсутнє <b>X хв / X год Y хв</b>`
- Офлайн:
  - `🔴 <b>Світло зникло</b>`
  - `⏰ Час зникнення: <b>HH:MM</b>`
  - `⏳ Світло було присутнє <b>X хв / X год Y хв</b>`

## Ключова поведінка
- Повідомлення надсилаються лише при зміні статусу (`online <-> offline`).
- Перше спостереження зберігає стан, але не надсилає повідомлення.
- Новий стан записується лише після успішного Telegram-запиту.
