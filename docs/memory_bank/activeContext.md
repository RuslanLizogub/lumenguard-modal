# Active Context

## Поточний стан
Проєкт у стабільному робочому стані: локальний режим перевірений, Modal Cron задеплоєний, повідомлення в Telegram надсилаються при зміні стану.

## Ядро логіки
- `logic.py`: TCP-перевірка, порівняння станів, форматування UA-повідомлень, JSON state helpers.
- `main.py`: оркестрація циклу, завантаження конфігурації, Telegram API, інтеграція з Modal (`Cron`, `Dict`, `Secret`).
- `tests/test_logic.py`: покриття основних сценаріїв логіки та форматування.

## Фактичний формат повідомлень
- Онлайн:
  - `🟢 <b>Світло з'явилося</b>`
  - `⏰ Час появи: <b>HH:MM</b>`
  - `⏳ Світло було відсутнє протягом <b>X хв / X год Y хв</b>`
- Офлайн:
  - `🔴 <b>Світло зникло</b>`
  - `⏰ Час зникнення: <b>HH:MM</b>`
  - `⏳ Світло було присутнє протягом <b>X хв / X год Y хв</b>`

## Ключова поведінка
- Повідомлення надсилаються лише при зміні статусу (`online <-> offline`).
- Перше спостереження зберігає стан, але не надсилає повідомлення.
- Новий стан записується лише після успішного Telegram-запиту.
